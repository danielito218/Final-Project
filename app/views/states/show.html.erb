<p style="color: green"><%= notice %></p>

<%= render @state %>

<div class="btn-group" role="group" aria-label="Basic example">
  <%= link_to "Edit this state", edit_state_path(@state), class: "btn btn-secondary"  %>&ensp;
  <%= link_to "Back to states", states_path, class: "btn btn-dark"   %>&ensp;

  <%= button_to "Destroy this state", @state, method: :delete, class: "btn btn-secondary"   %>
</div>

<p style="color: green"><%= notice %></p>

<%= render @state %>

<div class="btn-group" role="group" aria-label="Basic example">
  <%= link_to "", states_path, class: "btn btn-outline-dark bi bi-box-arrow-left"   %>&ensp;
  <%= link_to "", edit_state_path(@state), class: "btn btn-outline-dark bi bi-pencil-square"  %>&ensp;
  <%= button_to "", @state, method: :delete, class: "btn btn-outline-dark bi bi-trash3-fill"   %>
</div>

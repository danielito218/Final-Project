<p style="color: green"><%= notice %></p>

<%= render @status %>

<div class="btn-group" role="group" aria-label="Basic example">
  
  <%= link_to "", statuses_path, class: "btn btn-outline-dark bi bi-box-arrow-left"  %>&ensp;
  <%= link_to "", edit_status_path(@status), class: "btn btn-outline-dark bi bi-pencil-square" %>&ensp;
  <%= button_to "", @status, method: :delete, class: "btn btn-outline-dark bi bi-trash3-fill"  %>
  
</div>


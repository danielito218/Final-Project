<p style="color: green"><%= notice %></p>

<%= render @status %>

<div class="btn-group" role="group" aria-label="Basic example">
  <p><%= link_to "Edit this status", edit_status_path(@status), class: "btn btn-secondary" %>&ensp;
  <%= link_to "Back to statuses", statuses_path, class: "btn btn-dark"  %>&ensp;

  <%= button_to "Destroy this status", @status, method: :delete, class: "btn btn-secondary"  %><p>
</div>

